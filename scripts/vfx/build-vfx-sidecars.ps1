[CmdletBinding()] param()
throw 'Sidecar packaging requires a reviewed PyInstaller compatibility matrix. Development uses isolated virtual environments; no sidecar is produced until that matrix is completed.'
